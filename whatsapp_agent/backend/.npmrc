# Disable package-lock auto-generation during install
package-lock=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false

# Prefer offline for faster builds
prefer-offline=true
